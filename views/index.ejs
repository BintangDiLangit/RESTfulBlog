<% include partials/header %>

  <div class="ui grid">
    <div class="two wide column"></div>
    <div class="ui card twelve wide column">
      <div class="content">
        <div class="header"><i class="list icon" style="font-size: 2em"></i> Post Of Blog</div>
      </div>
      <div class="content">
        <% blog.forEach(function(data) { %>
          <div class="ui clearing segment">
            <div class="ui medium header"><%= data.title %></div>
            <img class="ui small left floated  image" src="<%= data.image %>">
            <span><i class="time icon"></i><strong><%= data.created.toDateString() %></strong></span>
            <p><%= data.body.substring(0, 100) %> ...</p><a class="ui inverted small green button" href="/detail/<%= data.id %>">Read More</a>
            <a class="ui inverted orange button" href="/edit-post/<%= data.id %>">Edit</a>
            <a class="ui inverted red button" href="/delete-post/<%= data.id %>">Delete</a>
          </div>
        <% }); %>
      </div>
    </div>

    <% include partials/footer %>
